/** Voir `flags.js` — flags MVP partagés backend/frontend. */
export declare const CHANNELS_ENABLED: {
  readonly email: boolean;
  readonly sms: boolean;
  readonly whatsapp: boolean;
  readonly push: boolean;
};

export declare const PHONE_AUTH_ENABLED: boolean;

export declare const BILLING_METHODS_ENABLED: readonly string[];
